# Identique à frontend/.streamlit/config.toml : actif si Streamlit est lancé depuis la racine.
[theme]
base = "light"
primaryColor = "#4f46e5"
backgroundColor = "#f5f7fb"
secondaryBackgroundColor = "#ffffff"
textColor = "#1e293b"
font = "sans serif"

[client]
toolbarMode = "minimal"
